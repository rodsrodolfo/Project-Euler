triangle(n) = n * (n + 1) / 2
isTriangle(num) = isinteger(√(num)) ? true : false

square(n) = n^2
isSquare(num) = isinteger(√num) ? true : false

pentagonal(n) = n * (3 * n - 1) / 2
isPentagonal(num) = isinteger((1 + √(1 + 24 * num)) / 6) ? true : false

hexagonal(n) = n * (2 * n - 1)
isTriangle(num) = isinteger(√(num)) ? true : false

heptagonal(n) = n * (5 * n - 3) / 2
isTriangle(num) = isinteger(√(num)) ? true : false

octagonal(n) = n * (3 * n - 2)
isTriangle(num) = isinteger(√(num)) ? true : false

# answer: 
# 
